this is a repo for making a driving alarm system. it uses four sensors to detect tiredness of the driver which in turn turns on an alarm and subsequent actions follow
Storing data in a dedicated cloud for now.
